<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Plantazzle! </title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>

      <body>
        <div class="wrapper">
          <header class="is-flex is-size-1 has-background-info is-justify-content-space-between">
            <h1 class="has-text-warning">
              <a href="/">P L A N T A Z Z L E</a>
            </h1>
            <nav class="navbar">
              <a href="/">Home</a>
              {{#if loggedIn}}
                <a href="/dashboard">dashboard</a>
                <button id="logout" class="btn-no-style">logout</button>
              {{else}}
                <a href="/login">login</a>
              {{/if}}
            </nav>
          </header>
    <main class="is-flex is-justify-content-center">
        {{{body}}}
    </main>
    <footer class="is-flex is-justify-content-center is-size-1 is-italic">

    </footer>
</div>

{{#if LogedIn}}
    <script src="/views/homepage.handlebars"></script>
{{/if}}

  <script src="/js/main.js"></script>
  <script src="/js/login.js"></script>
</body>


</html>