<div class="container">
  <div class="tile is-ancestor">
    <section class="tile is-vertical is-4">
      <div class="tile">
        <div class="tile is-parent is-vertical">
          {{#each plant1 as | plant |}}
            {{> plant/tile
              id=plant.PlantBasic.id
              title=plant.PlantBasic.botanicalName
              subtitle=plant.PlantBasic.commonName
              plantFileName=plant.PlantBasic.PlantPictures.0.filename }}
          {{/each}}
        </div>
      </div>
    </section>
    <section class="tile is-vertical is-4">
      <div class="tile">
        <div class="tile is-parent is-vertical">
          {{#each plant2 as | plant |}}
            {{> plant/tile
              id=plant.PlantBasic.id
              title=plant.PlantBasic.botanicalName
              subtitle=plant.PlantBasic.commonName
              plantFileName=plant.PlantBasic.PlantPictures.0.filename }}
          {{/each}}
        </div>
      </div>
    </section>
    <section class="tile is-vertical is-4">
      <div class="tile">
        <div class="tile is-parent is-vertical">
          {{#each plant3 as | plant |}}
            {{> plant/tile
              id=plant.PlantBasic.id
              title=plant.PlantBasic.botanicalName
              subtitle=plant.PlantBasic.commonName
              plantFileName=plant.PlantBasic.PlantPictures.0.filename }}
          {{/each}}
        </div>
      </div>
    </section>
  </div>
</div>
{{!-- <button id="chat" class="button is-success is-focused">Chat</button> --}}
<div id="usermenu" class="dropdown is-up is-right is-hoverable">
  <div class="dropdown-trigger">
    <button class="button is-success" aria-haspopup="true" aria-controls="dropdown-menu">
      <span>User Menu</span>
      <span class="icon is-small">
        <i class="fas fa-angle-up" aria-hidden="true"></i>
      </span>
    </button>
  </div>
  <div class="dropdown-menu" id="dropdown-menu" role="menu">
    <div class="dropdown-content">
      <a href="" class="dropdown-item">
        Chat
      </a>
      <a href="" class="dropdown-item">
        Add a new Plant
      </a>
      <a href="" class="dropdown-item">
        Add my Plant
      </a>
    </div>
  </div>
</div>

{{> plant/readPlantBasics }}
{{> modals/chat}}