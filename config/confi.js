
export const database =
{
    host: process.env.DATABASE_HOST || "localhost",
    user: process.env.DATABASE_USER || "root",
    password: process.env.DATABASE_PASSWORD || "Jones2705!",
    database: process.env.DATABASE_NAME || "dblinks",
}